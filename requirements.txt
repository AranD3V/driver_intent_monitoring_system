numpy>=1.21.0
opencv-python>=4.8.0
torch>=2.1.0
torchvision>=0.16.0
mediapipe>=0.10.30
ultralytics>=8.1.0
PyYAML>=6.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
Pillow>=10.0.0
metadrive-simulator>=0.4.1.1,<0.5.0
carla==0.9.16

# Optional: enables spoken driver-assist prompts via Windows SAPI / macOS
# NSSpeech / espeak. Falls back to severity-coded beeps + console logging
# if not installed.
pyttsx3>=2.90; platform_system == "Windows"
